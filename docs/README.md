# AutoLuno Docs

The main project guide is `docs/ReadMe.html`.

Use it as the current source of truth for:

- app progress
- safety boundaries
- runtime map
- SQLite data map
- cleanup plan
- file map

Older root docs remain historical until they are consolidated.
