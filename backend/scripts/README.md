# Backend scripts

Place focused operator, ingestion, and development utilities here. Prefer
subfolders such as `dev/`, `ingestion/`, or `maintenance/`. Keep reusable
runtime logic under `backend/app/` so scripts remain thin entry points.
